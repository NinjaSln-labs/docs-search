# 安全策略

## 报告漏洞

发现安全漏洞请**不要**开公开 Issue。请使用 GitHub 私密安全通告：

1. 仓库页 → **Security** → **Report a vulnerability**
2. 或邮件联系仓库所有者（见 GitHub 主页）

请在报告中附：影响范围、复现步骤、可能的修复思路。收到后 **72 小时内**确认，修复窗口按严重度协商。

## 安全设计要点

- 服务默认仅监听 `127.0.0.1`；**监听非回环地址（如 `--host 0.0.0.0`）必须启用认证**，否则拒绝启动
- 认证（远端部署，二选一）：`--token`（Bearer）或 `--user/--password`（Basic），保护全部路径；
  凭据比较用 `hmac.compare_digest`（防时序侧信道）；凭据只放启动参数/环境变量，勿入库
- 上传接口：仅 `.md`、单文件 ≤10MB、文件名消毒（防路径穿越）、UTF-8 校验；同名默认提示（409），
  覆盖/新文件需显式 `if_exists`
- 删除接口：仅允许 `uploads/` 目录内文件（防任意文件删除）
- 所有 SQL 均为参数化查询（`?` 占位符），用户输入不参与结构拼接——无 SQL 注入面
- 本地模式无遥测、无网络请求、索引库仅存本地；MCP/CLI 远程模式（`--url`）会向指定服务发 HTTP 请求，
  仅连接可信服务，凭据经 `--token`/`--user`/环境变量传递

## 已知边界

- HTTP 服务为标准库 `http.server`，未做生产级加固（请求限速/TLS），定位是**本机个人工具**
- LIKE 查询对超大文档库（>10 万篇）未做性能优化
- `workspace` 库（`~/.docs-search/workspaces/<ws>/`）与默认库隔离，但均为本机可读——勿写入密钥/隐私原始数据
